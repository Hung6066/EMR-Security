export const environment = {
  production: false,
  apiUrl: 'http://localhost:5000/api', // Hoặc port của backend khi chạy dev
  recaptcha: {
    siteKey: 'your-dev-recaptcha-site-key' // Sử dụng key test của Google hoặc key dev của bạn
  }
};